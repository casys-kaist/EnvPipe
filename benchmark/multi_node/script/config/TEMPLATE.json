{
    "train_batch_size": CONFIG_BATCH_SIZE,
    "train_micro_batch_size_per_gpu": CONFIG_MBSIZE,
    "optimizer": {
        "type": "Adam",
        "params": {
            "lr": 0.001,
            "betas": [
                0.9,
                0.999
            ],
            "eps": 1e-8
        }
    },
    "steps_per_print": 1,
    "wall_clock_breakdown": false,
    "envpipe": {
        "type": "ENVPIPE_TYPE",
        "scheduling": "ENVPIPE_SCHEDULING",
        "reconfig": "ENVPIPE_RECONFIGURE",
        "gpu": "ENVPIPE_GPU",
        "reconfigure_threshold_scale": "ENVPIPE_RECONFIG_THRESHOLD_SCALE"
    }
}